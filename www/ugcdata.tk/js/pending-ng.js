const info = {
    page: "pending.html",
    path: "/report/pending/"
};
Object.freeze(info);

$(window).ready(function () {
    scope = angular.element(document.getElementById("ng-app")).scope();
});